{{> ticket-header}}

<div class="ticketBottom row">

      <div class="flex col-sm-12 col-md-6">
        <div class="card">
          <div class="card-body bg-secondary">
            <form id="ticketInfo">

            <h2>{{ticket.ticket_title}}</h2>

            <label class="form-label mt-2">Subject</label>
            <input class="form-control" type="text" id="ticketSubject"  value="{{ticket.ticket_title}}" >

            <label class="form-label mt-2">Description</label>
            <textarea class="form-control" id="ticketDescription" rows="3" placeholder="{{ticket.ticket_text}}">{{ticket.ticket_text}}</textarea>
            <label class="form-label mt-2">Urgency</label>
                {{{renderUrgencyOptions ticket.urgency}}}

            <label class="form-label mt-2">Status</label>
                {{{renderStatus ticket.status}}}

            <button type="submit" class="mt-3 btn btn-primary font-weight-bold" id="saveChanges">Save Changes</button>

            </form>
          </div>
        </div>
      </div>

    <div class="flex col-sm-12 col-md-6">
        <div class="container py-5">
                <div class="row">
                    <div class="col-lg-7 mx-auto">
                        <ul class="timeline">
                            {{#each ticket.logs as |log|}}
                              <li class="timeline-item rounded ml-3 p-4 shadow">
                                <div data-id="{{log.id}}">{{ticket_id}}</div>
                                  <div class="timeline-arrow"></div>
                              </li>
                              <i class="fa fa-clock-o mr-1 ml-3"></i><span data-id="{{ticket_id}}" class="small text-gray">{{{ format_date date_created}}}</span>
                            {{/each}}
                        </ul>
                    </div>
                </div>
            </div>
    </div>

</div>

<a href="/">
<button type="click" class="text-white bg-primary font-weight-bold mx-auto mb-4 rounded" id="backBtn"><i class=" mr-2 fa-solid fa-arrow-left"></i>Back</button></a>
</a>
<script src = '/js/ticket.js'></script>